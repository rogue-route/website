/**
 * Quiz submit API — placeholder.
 * Real storage wired in Task 6.2 (Supabase).
 */
export async function POST() {
  return Response.json({ ok: true, message: "Placeholder — Task 4.8 wires this up." });
}
