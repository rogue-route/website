/**
 * Waitlist API — placeholder.
 * Real storage wired in Task 6.2 (Supabase/Resend).
 */
export async function POST() {
  return Response.json({ ok: true, message: "Placeholder — Task 2.3 wires this up." });
}
